spring.application.name=bowling-hall-test
spring.config.import=optional:env.test.properties
spring.datasource.url=${JDBC_TEST_DATABASE_URL}
spring.datasource.username=${JDBC_TEST_DATABASE_USERNAME}
spring.datasource.password=${JDBC_TEST_DATABASE_PASSWORD}
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update